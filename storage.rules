rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Only Cloud Functions can write; signed URLs handle reads
    match /downloads/{allPaths=**} {
      allow read, write: if false;
    }
  }
}

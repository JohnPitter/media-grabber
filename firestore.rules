rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client reads/writes - only Cloud Functions have access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
